scenario:
- step:
  class: PostgresRead
  arguments:
    io: input
    host: localhost
    user: postgres
    dbname: postgres
    tblname: tblname
    raw_query: SELECT * FROM test
    password: password
   #port: 5432
- step:
  class: CsvWrite
  io: output
  arguments:
    dest_path: /usr/local/sample/test_{{ today }}_.csv
    with_vars:
     today: date '+% Y%m%d'
 
- step:
  class: CsvReadPostgresCreate
  arguments:
    host: localhost
    user: postgres
    dbname: postgres
    tblname: iris
    password: P@ssw0rd
    src_dir: /Users/shakemaru
    src_pattern: iris2.csv
    columns:
    #  - column1
    #  - column2
      